import 'package:flutter/material.dart';

class AddExercisePlanScreen extends StatelessWidget {
  const AddExercisePlanScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Add Plan');
  }
}
